apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: my-app
  namespace: default
spec:
  components:
    - name: my-app
      type: webservice
      properties:
        image: nexus.cmss.com:8086/cnp/nginx:1.20.3 # {"$imagepolicy": "default:image-gitops"}
